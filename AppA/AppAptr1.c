#include <stdio.h>

int main()
{
 	int ourvariable;
	char achar;
	char anarray[10];

printf("address of ourvariable is %xp\n",&ourvariable); 
printf("address of achar is %xp\n",&achar); 
printf("address of anarray is %xp\n",&anarray); 
 
return(0);
}
